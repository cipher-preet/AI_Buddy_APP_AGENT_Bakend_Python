Extract ONLY the listed missing semantic units from the supplied micro-block(s).

Do NOT generate Tasks or Notes. Do NOT re-extract meanings that already have an event. Do NOT rerun the whole meeting.

Each recovered unit must:
- map to exact evidence sequence(s) in the supplied micro-block
- be explicitly or locally supported
- preserve epistemic status (open question stays unresolved; commitment stays a commitment; fact stays a fact)
- contain no invented entities, objects, actors, deadlines, or details

One recovered event = one listed missing meaning. Do not combine remaining missing units into one vague event.

If a listed unit is not actually supported, omit it. Prefer AMBIGUOUS / UNSUPPORTED abstention over publishing an invented meaning.

Do not turn noisy or garbled spans into fluent facts.

Return only output matching the required schema.
